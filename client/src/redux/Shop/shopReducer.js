// create shop reducer
const shopReducer = (state = 0, { type, payload }) => {
  switch (type) {
    case "":
      return;

    default:
      return state;
  }
};

// export shop reducer
export default shopReducer;
